# Vite exposes only variables prefixed with VITE_
# Do NOT commit real keys. Put your actual key in a local `.env` (gitignored) or via in-app Settings.
VITE_DEEPGRAM_API_KEY=your_deepgram_api_key_here
